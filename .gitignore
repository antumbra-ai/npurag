# Rust
/target
**/*.rs.bk

# npurag local index / database artifacts (never commit these)
*.db
*.db-shm
*.db-wal

# Release staging, produced by the release workflow and the Inno Setup script
/dist

# OS / editor
.DS_Store
.idea/
.vscode/

# Note: npurag is a binary crate — DO commit Cargo.lock (it is intentionally not ignored).
